name: Playwright site availability

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *' # daily
  push:
    branches:
      - main

jobs:
  playwright-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies and Playwright
        run: |
          npm init -y
          npm install -D @playwright/test
          npx playwright install --with-deps
      - name: Run Playwright tests
        run: |
          npx playwright test tests/playwright --reporter=github

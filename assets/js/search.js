// Lunr search configuration
document.addEventListener('DOMContentLoaded', function() {
  // Check if we're on the homepage
  if (document.getElementById('skillsGrid')) {
    // Homepage search is handled by inline JavaScript in home.html
    // This file is here for future expansion (e.g., full-text search)
  }
});
